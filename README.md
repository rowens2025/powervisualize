# PowerVisualize – React + Vite + Tailwind (Ready for Vercel)

## Quickstart
```bash
npm install
npm run dev
```

## Deploy to Vercel
- Import repo → Deploy (Vite detected)
- Build: `npm run build` → Output: `dist`

## Paste your report links
Edit `src/App.tsx` → the `reports` array.
